import { Builder } from "@/features/builder";

export const metadata = {
    title: "Builder | Dashboard",
    description: "Manage your Flow builder",
};

export default function BuilderPage() {
    return <Builder />;
}
