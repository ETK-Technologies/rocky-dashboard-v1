import { GlobalAttributes } from "@/features/attributes";

export const metadata = {
  title: "Global Attributes | Dashboard",
  description: "Manage global product attributes",
};

export default function GlobalAttributesPage() {
  return <GlobalAttributes />;
}
